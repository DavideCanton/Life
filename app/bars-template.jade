doctype html
div.sidebar-links
	button.btn(ng-repeat="bar in bars track by $index", ng-click="toggle(bar)",
				ng-class="{'btn-danger': bar.selected, 'btn-primary': !bar.selected}") {{ bar.title }}

div(ng-transclude)
